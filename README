# ProtApp

Client App for ProtOs, written in Python using Kivy library
https://github.com/zgrate/ProtOs

### Supported devices:

- Z-Grate configuration (Default config in repo)

# Requirements
- Python 3.7
- Device Compatible with ProtOs


# Installation
```Python
pip3 install -r requirements.txt
python3 main.py
```

